package com.ntels.ccbs.batch.common.service;

public interface BillingUtilService {

	/**
	 * <pre>
	 * 청구금액의 마지막 자릿수에 대한 조정을 수행한다.
	 * 예 : 원단위 절삭
	 * </pre>
	 * @param value
	 * @return
	 */
	double adjustLastDigit(double value);
	
}
